#ifndef LABA1_RANDOM_H
#define LABA1_RANDOM_H

#include <cstdlib>
#include <ctime>

#include "Definition.h"

long randomIn(long from, long to);
int randomIntIn(int from, int to);
double randomDoubleIn(double from, double to);
String randomStrGen(int length);

#endif //LABA1_RANDOM_H
